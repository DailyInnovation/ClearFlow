kind = "web"
previewPath = "/"
title = "Contextual Checklist"
version = "1.0.0"
id = "artifacts/checklist"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21472

[services.development]
run = "pnpm --filter @workspace/checklist run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/checklist", "run", "build" ]
publicDir = "artifacts/checklist/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21472"
BASE_PATH = "/"
